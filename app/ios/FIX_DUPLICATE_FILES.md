# 🔧 Исправление: "Multiple commands produce" ошибка

## 🔴 Проблема
```
Multiple commands produce '.../Muzic2App.app/audio.wav'
Multiple commands produce '.../Muzic2App.app/index.php'
```

Файлы `audio.wav` и `index.php` добавлены в проект дважды:
1. Как часть папки `content` (автоматически через File System Synchronized Group)
2. Отдельно в Build Phases → Copy Bundle Resources

## ✅ Решение

### Шаг 1: Откройте Build Phases

1. **В Xcode:**
   - Выберите **проект** в навигаторе (синий иконка сверху)
   - Выберите **Target "Muzic2App"**
   - Откройте вкладку **"Build Phases"**

### Шаг 2: Проверьте "Copy Bundle Resources"

1. **Разверните секцию "Copy Bundle Resources"**
2. **Найдите файлы:**
   - `audio.wav`
   - `index.php`
3. **Если они там есть:**
   - Выберите каждый файл
   - Нажмите клавишу **Delete** или кнопку **"-"** внизу
   - Подтвердите удаление

### Шаг 3: Проверьте другие секции

Также проверьте:
- **"Compile Sources"** - там не должно быть этих файлов
- **"Link Binary With Libraries"** - там не должно быть этих файлов

### Шаг 4: Убедитесь, что папка content правильно добавлена

1. **В навигаторе проекта** проверьте папку `content`:
   - Она должна быть **синей** 🔵 (Folder Reference)
   - Или использовать **File System Synchronized Group** (Xcode 15+)

2. **Проверьте Target Membership:**
   - Выберите папку `content`
   - Откройте File Inspector (правая панель)
   - Убедитесь, что стоит галочка напротив "Muzic2App"

### Шаг 5: Очистите и пересоберите

1. **Product → Clean Build Folder** (`Shift + Cmd + K`)
2. **Product → Build** (`Cmd + B`)
3. Ошибка должна исчезнуть

---

## 🔍 Альтернативное решение (если файлы не в Build Phases)

Если файлы не видны в Build Phases, но ошибка остаётся:

### Вариант 1: Удалите файлы из корня проекта

Файл `audio.wav` находится в корне проекта (`/Applications/MAMP/htdocs/muzic2/audio.wav`). Если он не нужен в корне:

1. **Удалите его из проекта Xcode** (если добавлен)
2. Или **переместите** в папку `content`, если он нужен в приложении

### Вариант 2: Исключите файлы из синхронизации

Если используете File System Synchronized Group (Xcode 15+):

1. Выберите папку `content` в навигаторе
2. Откройте File Inspector
3. В настройках синхронизации можно исключить конкретные файлы

---

## ⚠️ Важно

**Не удаляйте файлы из папки `content` на диске!** Удаляйте только ссылки из Build Phases в Xcode.

Файлы должны быть **только** в папке `content`, и копироваться автоматически через Folder Reference или File System Synchronized Group.

---

## ✅ Проверка после исправления

После удаления дубликатов:
- ✅ Сборка должна проходить без ошибок
- ✅ Файлы должны быть в bundle (как часть папки content)
- ✅ Приложение должно находить файлы

---

**Если проблема остаётся**, проверьте:
1. Нет ли файлов в корне проекта, которые конфликтуют
2. Правильно ли настроена папка content (синяя или синхронизированная)
3. Нет ли других дубликатов в Build Phases


