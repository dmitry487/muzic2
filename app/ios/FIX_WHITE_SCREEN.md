# 🔧 Исправление белого экрана в iOS приложении

## ✅ Что было исправлено:

1. **Создан `index.html`** - приложение теперь ищет правильный файл
2. **Улучшен AppDelegate.swift** - добавлено логирование и обработка ошибок
3. **Исправлена обработка API** - в app mode используются JSON файлы вместо PHP
4. **Улучшена конфигурация WKWebView** - правильные настройки для локальных файлов

## 📋 Шаги для исправления:

### 1. Скопируйте обновленный контент в Xcode проект:

```bash
cd /Applications/MAMP/htdocs/muzic2/app
npm run copy-content
```

### 2. В Xcode:

1. **Удалите старую папку `content` из проекта:**
   - В навигаторе проекта найдите папку `content`
   - Правой кнопкой → Delete
   - Выберите "Move to Trash"

2. **Добавьте обновленную папку `content`:**
   - Правой кнопкой на папке `Muzic2App` → **Add Files to "Muzic2App"...**
   - Выберите `app/content/` (или `ios/Muzic2App/Muzic2App/content/` если скопировали туда)
   - ✅ Убедитесь, что выбран Target "Muzic2App"
   - ✅ Выберите **"Create folder references"** (НЕ "Create groups"!)
   - ✅ НЕ ставьте галочку "Copy items if needed" (если файлы уже на месте)
   - Нажмите "Add"

3. **Проверьте, что `index.html` есть в папке `content`:**
   - В навигаторе проекта откройте папку `content`
   - Должен быть файл `index.html` (не только `index.php`)

### 3. Пересоберите приложение:

1. **Product → Clean Build Folder** (Shift+Cmd+K)
2. **Product → Build** (Cmd+B)
3. **Запустите на iPhone** (Cmd+R)

### 4. Проверьте логи в Xcode:

1. Откройте **Console** в Xcode (View → Debug Area → Activate Console)
2. Запустите приложение
3. Ищите сообщения:
   - ✅ `📦 Resource Path: ...`
   - ✅ `📁 Content Path: ...`
   - ✅ `✅ Папка content существует`
   - ✅ `✅ index.html найден`
   - ✅ `🌐 Загружаем URL: ...`
   - ✅ `✅ Страница загружена успешно`

Если видите ошибки (❌), проверьте:
- Существует ли папка `content` в bundle
- Есть ли файл `index.html` в папке `content`
- Правильно ли добавлена папка как "folder reference" (синий цвет)

### 5. Если всё ещё белый экран:

1. **Проверьте консоль браузера:**
   - В Xcode: Window → Devices and Simulators
   - Выберите ваше устройство
   - Нажмите "Open Console" для приложения
   - Ищите JavaScript ошибки

2. **Проверьте пути к ресурсам:**
   - Убедитесь, что все пути в `index.html` относительные (начинаются с `./` или без `/`)
   - Проверьте, что CSS и JS файлы загружаются

3. **Проверьте JSON API файлы:**
   - Убедитесь, что в папке `content/api/` есть файлы:
     - `home.json`
     - `tracks.json`
     - `likes.json`
     - `user.json`
     - и т.д.

## 🐛 Отладка:

### Включите Web Inspector (iOS 16.4+):

1. В Xcode: Window → Devices and Simulators
2. Выберите ваше устройство
3. Найдите приложение Muzic2
4. Нажмите "Open Console"
5. Или используйте Safari → Develop → [Ваш iPhone] → Muzic2

### Проверьте загрузку файлов:

В консоли браузера выполните:
```javascript
fetch('./api/home.json').then(r => r.json()).then(console.log).catch(console.error)
```

Если ошибка - проверьте пути к файлам.

## ✅ Ожидаемый результат:

После исправлений приложение должно:
1. Загружать `index.html` без ошибок
2. Показывать интерфейс (не белый экран)
3. Загружать данные из JSON файлов
4. Работать полностью офлайн

---

**Если проблема остаётся**, пришлите логи из Xcode Console и Safari Web Inspector.




