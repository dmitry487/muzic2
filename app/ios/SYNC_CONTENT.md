# 🔄 Синхронизация контента для iOS проекта

## Проблема
После обновления файлов в `app/content/`, они не автоматически копируются в `app/ios/Muzic2App/Muzic2App/content/`.

## ✅ Решение

### Автоматическая синхронизация

Выполните команду:
```bash
cd /Applications/MAMP/htdocs/muzic2/app
npm run copy-content
cp -r content/* ios/Muzic2App/Muzic2App/content/
```

### Или вручную:

```bash
# Скопировать все файлы из app/content в iOS проект
cp -r /Applications/MAMP/htdocs/muzic2/app/content/* \
      /Applications/MAMP/htdocs/muzic2/app/ios/Muzic2App/Muzic2App/content/
```

## 📋 После копирования:

1. **Откройте Xcode**
2. **Product → Clean Build Folder** (`Shift + Cmd + K`)
3. **Product → Build** (`Cmd + B`)
4. **Product → Run** (`Cmd + R`)

---

## ⚠️ Важно

Папка `content` в iOS проекте должна быть добавлена как **Folder Reference** (синяя папка), чтобы изменения файлов автоматически отражались в проекте.

Если папка желтая (Group), удалите и добавьте заново как Folder Reference.


