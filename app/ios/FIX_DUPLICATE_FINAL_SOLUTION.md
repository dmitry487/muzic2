# ✅ Окончательное решение: "Multiple commands produce"

## 🔴 Проблема
Файлы `audio.wav` и `index.php` копируются в корень bundle из разных подпапок, вызывая конфликт:
- `audio.wav` из `tracks/music/audio.wav` и `tracks/music/corpus/audio.wav`
- `index.php` из `content/index.php` и `content/admin/index.php`

## ✅ Решение (ВЫПОЛНЕНО)

Я заменил **File System Synchronized Group** на обычный **Folder Reference**, который сохраняет структуру папок при копировании.

### Что было изменено:

1. ✅ Заменен `PBXFileSystemSynchronizedRootGroup` на `PBXFileReference` с типом `folder`
2. ✅ Удалена секция `fileSystemSynchronizedGroups` из target
3. ✅ Добавлена папка `content` в `PBXResourcesBuildPhase` для копирования в bundle

### Теперь нужно:

1. **Закройте Xcode** (если открыт)
2. **Откройте проект заново**
3. **Product → Clean Build Folder** (`Shift + Cmd + K`)
4. **Product → Build** (`Cmd + B`)

---

## ✅ Результат

Теперь папка `content` будет копироваться в bundle **со всей структурой папок**, поэтому:
- ✅ `tracks/music/audio.wav` → `Muzic2App.app/content/tracks/music/audio.wav`
- ✅ `tracks/music/corpus/audio.wav` → `Muzic2App.app/content/tracks/music/corpus/audio.wav`
- ✅ `index.php` → `Muzic2App.app/content/index.php`
- ✅ `admin/index.php` → `Muzic2App.app/content/admin/index.php`

**Конфликта больше не будет**, так как файлы находятся в разных путях в bundle!

---

## 🔍 Проверка

После сборки проверьте:
1. ✅ Ошибка "Multiple commands produce" должна исчезнуть
2. ✅ Папка `content` должна быть в bundle со всей структурой
3. ✅ Приложение должно находить файлы по правильным путям

---

## ⚠️ Если проблема остаётся

Если ошибка всё ещё появляется:

1. **Проверьте Build Phases:**
   - Проект → Target "Muzic2App" → Build Phases
   - В "Copy Bundle Resources" должна быть только папка `content` (синяя иконка)
   - Если там есть отдельные файлы `audio.wav` или `index.php` - удалите их

2. **Проверьте навигатор проекта:**
   - Папка `content` должна быть **синей** 🔵 (Folder Reference)
   - Если она желтая 🟡 - удалите и добавьте заново как Folder Reference

3. **Очистите DerivedData:**
   ```bash
   rm -rf ~/Library/Developer/Xcode/DerivedData/Muzic2App-*
   ```

---

**Готово!** Теперь проект должен собираться без ошибок.


