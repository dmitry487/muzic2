# Muzic2 Desktop & Mobile App

Независимое нативное приложение для macOS и iOS, работающее полностью офлайн без веб-сервера.

## 🎯 Особенности

- ✅ **Полная независимость** - приложение не влияет на основной сайт
- ✅ **Офлайн работа** - все файлы включены в приложение
- ✅ **Нативный интерфейс** - использует системные компоненты
- ✅ **Автоматическая адаптация** - пути и API автоматически настраиваются

## 📁 Структура

```
app/
├── electron/              # Electron приложение для macOS
│   └── main.js            # Главный файл с локальным сервером
├── ios/                   # iOS приложение (Swift + WKWebView)
│   └── Muzic2App/         # Xcode проект
├── content/               # Контент сайта (создается автоматически)
│   ├── assets/            # CSS, JS, изображения
│   ├── tracks/            # Музыкальные файлы и обложки
│   └── api/               # JSON файлы для эмуляции API
├── scripts/               # Скрипты для сборки
│   ├── copy-content.js           # Копирование контента
│   ├── adapt-js-for-app.js      # Адаптация JS файлов
│   └── generate-api-json.js     # Генерация API JSON
└── package.json           # Конфигурация npm
```

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
cd app
npm install
```

### 2. Подготовка контента

Скопируйте весь контент сайта в приложение:

```bash
npm run copy-content
```

Этот скрипт автоматически:
- Копирует файлы из `public/` в `app/content/`
- Копирует треки из `tracks/` в `app/content/tracks/`
- Адаптирует все пути в HTML, JS и CSS
- Генерирует базовые API JSON файлы

### 3. Запуск

#### macOS (Electron)

```bash
npm start
```

#### Сборка установщика

```bash
npm run build:mac
```

Установщик будет в папке `dist/`

### 4. iOS

См. подробные инструкции в [ios/README.md](ios/README.md)

## 🔧 Технические детали

### macOS (Electron)

- Использует встроенный Express сервер для обслуживания контента
- Все файлы упакованы в приложение
- API эмулируется через статические JSON файлы
- Работает на любом порту (автоматически выбирается свободный)

### iOS

- Использует WKWebView для отображения веб-контента
- Все файлы включены в bundle приложения
- Работает через file:// протокол
- Поддерживает все функции веб-версии

## 📝 Важные замечания

1. **Независимость**: Все файлы приложения находятся в папке `app/` и не влияют на основной сайт

2. **API**: В режиме приложения API работает через статические JSON файлы. Для реальных данных нужно экспортировать их из базы данных

3. **Размер приложения**: Все музыкальные файлы копируются в приложение, что значительно увеличивает его размер

4. **Обновления**: Для обновления контента запустите `npm run copy-content` снова

## 📚 Дополнительная документация

- [INSTALL.md](INSTALL.md) - Подробная инструкция по установке
- [ios/README.md](ios/README.md) - Инструкции для iOS приложения

## 🐛 Решение проблем

### Приложение не запускается
- Убедитесь, что установлены зависимости: `npm install`
- Проверьте наличие контента: `ls content/`

### API не работает
- Проверьте JSON файлы: `ls content/api/`
- Запустите генерацию: `node scripts/generate-api-json.js`

### iOS не загружает контент
- Убедитесь, что папка `content/` добавлена в Xcode как "folder reference"
- Проверьте пути в `AppDelegate.swift`

