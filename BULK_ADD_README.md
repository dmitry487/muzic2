# 🚀 Быстрое массовое добавление треков

## Способы добавления

### 1. Через командную строку (САМЫЙ БЫСТРЫЙ) ⚡

```bash
# Добавить все MP3 из папки tracks/music/
php bulk_add_tracks.php

# Или указать другую папку
php bulk_add_tracks.php /path/to/music/folder/
```

**Преимущества:**
- ✅ Автоматическое извлечение метаданных из MP3
- ✅ Пакетная вставка в БД (50 треков за раз)
- ✅ Автоматическое извлечение обложек
- ✅ Очень быстро (100+ треков за секунды)

### 2. Через веб-интерфейс 🌐

Откройте: `/muzic2/public/admin/bulk_upload.html`

**Возможности:**
- Drag & Drop файлов
- Автоматическое извлечение метаданных
- Прогресс-бар
- Статистика добавления

### 3. Старый способ (медленный) 🐌

Через админ-панель по одному треку за раз.

## Что извлекается автоматически

- ✅ **Название трека** (Title) - из MP3 тегов или iTunes API
- ✅ **Артист** (Artist) - из MP3 тегов или iTunes API
- ✅ **Альбом** (Album) - из MP3 тегов или iTunes API
- ✅ **Длительность** (Duration) - из MP3 файла или iTunes API
- ✅ **Обложка** (Cover) - из MP3, папки или iTunes API (600x600)
- ✅ **Жанр и год** - из iTunes API (если доступно)

## Требования

1. **getID3** (опционально) - для извлечения метаданных из файлов
   ```bash
   composer require james-heinrich/getid3
   ```
   ⚠️ Если getID3 не установлен, будет использоваться только API (iTunes, MusicBrainz)

2. **PHP** с расширениями:
   - PDO
   - SQLite/MySQL
   - fileinfo
   - curl (для работы с API)

## Пример использования

```bash
# 1. Поместите MP3 файлы в tracks/music/
cp ~/Music/*.mp3 /Applications/MAMP/htdocs/muzic2/tracks/music/

# 2. Запустите скрипт
cd /Applications/MAMP/htdocs/muzic2
php bulk_add_tracks.php

# 3. Готово! Треки добавлены в базу
```

## Производительность

- **CLI скрипт**: ~100-200 треков/сек
- **Веб-интерфейс**: ~10-20 треков/сек (зависит от размера файлов)
- **Старый способ**: ~1 трек/сек

## Оптимизации

1. **Пакетная вставка** - вставка по 50 треков за раз вместо одного
2. **Транзакции** - все треки добавляются в одной транзакции
3. **Автоматические метаданные** - не нужно вводить вручную
4. **Проверка дубликатов** - автоматически пропускает уже добавленные треки

## Решение проблем

### getID3 не найден
```bash
composer install
# или
composer require james-heinrich/getid3
```

### Файлы не добавляются
- Проверьте права на папку `tracks/music/`
- Проверьте права на папку `tracks/covers/`
- Проверьте логи ошибок PHP

### Медленная работа
- Используйте CLI скрипт вместо веб-интерфейса
- Увеличьте `batchSize` в `bulk_add_tracks.php` (строка 30)

